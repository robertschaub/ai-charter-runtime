<!-- SPDX-License-Identifier: CC-BY-4.0 -->
# Claude Code — ai-charter-runtime

@AGENTS.md

Read AGENTS.md directly if the import was not loaded, then applicable nested rules. The root owns spec authority, gate invariants, licensing, secrets/data, record integrity and operation boundaries; this adapter is subordinate.

The repository's PreToolUse Git-safety hook is a backstop when loaded/trusted. Verify effective client/version behavior; no defaultMode in tracked settings proves neither a prompt nor confinement. Do not infer that another client's hooks apply. Destructive/irreversible actions remain main-session-only.

The existing Claude verify definition declares Read/Grep/Glob and `model: inherit`. Its installed tool restriction must be verified before reliance; return missing checks in chat without broadening tools. Writers need an explicit state/command contract; the integrator supplies concurrent worktrees and handles shared records/Git integration. Restricted rollout writers return only edits/evidence.

Current authorization must cover probes, keys, signing, provider spending and external mutation. Existing authority persists; review preparation does not create it. Read-only reviews consume captured evidence; reproduction needs declared writable state. Use supported configured model/effort, Windows/PowerShell-compatible commands and the repository's LF/SPDX rules.
